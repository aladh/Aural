import SwiftUI

/// Small, shared surfaces for the Spotify-inspired shell. The app deliberately keeps its
/// account and accent preferences intact; these are only the neutral dark surfaces behind them.
enum SpotifyShellStyle {
    static let canvas = Color(red: 0.055, green: 0.063, blue: 0.071)
    static let sidebar = Color(red: 0.078, green: 0.086, blue: 0.098)
}
